# Database Configuration
DB_PATH=/app/db/claude.db

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Configuration (comma-separated origins, or * for all)
CORS_ORIGINS=*

# Logging
LOG_LEVEL=info

# Projects Directory (for workspace mounting)
PROJECTS_DIR=/projects

# Session Tracking (for hook system)
CLAUDE_SESSION_ID=1
CLAUDE_AGENT_ID=default

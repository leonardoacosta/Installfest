[package]
name = "cmux-bridge"
version = "0.1.0"
edition = "2021"
description = "HTTP-to-cmux proxy for remote machines over Tailscale"

[dependencies]
tiny_http = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
lto = true
